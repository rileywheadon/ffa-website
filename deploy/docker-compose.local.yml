services:
  caddy:
    volumes:
      - ./Caddyfile.local:/etc/caddy/Caddyfile:ro
    ports:
      - "8080:80" 

  # live-mount your working tree (optional but convenient)
  flask:
    volumes:
      - ..:/app
    ports:
      - "5000:5000"

  # expose Plumber for testing only (not used in production)
  plumber:
    ports:
      - "8000:8000"

